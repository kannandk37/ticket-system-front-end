1. Install the dependencies
2. in vite.config.ts - comment out 'base' if running in local
3. Add the api url in .env file
4. Run command - npm run build
5. Run watch command to see any type error - npx tsc -watch
6. Run project - npm run dev
